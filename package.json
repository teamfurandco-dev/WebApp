{
  "name": "fur-and-co-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "deploy": "cd apps/webapp && pnpm build && gh-pages -d dist",
    "load-data": "node scripts/load-realistic-data.js",
    "migrate-variants": "node scripts/apply-variant-migration.js",
    "test-variants": "node scripts/test-variant-system.js",
    "setup-production": "pnpm load-data && echo 'Production data loaded successfully!'"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {
    "@prisma/client": "5.22.0",
    "@supabase/supabase-js": "^2.95.0",
    "dotenv": "^17.2.3",
    "prisma": "5.22.0"
  }
}

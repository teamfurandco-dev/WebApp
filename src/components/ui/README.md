# shadcn/ui Components Directory

This directory will contain your shadcn/ui components.

To add components, use:
```bash
npx shadcn@latest add button
npx shadcn@latest add card
npx shadcn@latest add dialog
# ... etc
```

Components will be automatically configured and ready to use!
